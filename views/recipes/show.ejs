<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= Recipe.name %>
  </title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>

    <h1>A recipes name: <%= Recipe.name %>
    </h1>

    <h2>Details</h2>
    <p>recipes instructions: <%= Recipe.instructions %>
    </p>

    <h2>Ingredients</h2>
    <ul>

      <% ingredients.forEach((ingredient)=> { %>
        <li>
          <%= ingredient.name %>
        </li>
        <% }) %>
    </ul>
    <br>

    <h2>Owner</h2>
    <% if (Recipe.owner._id.equals(user._id)) { %>
      <p>You own this!</p>
      <form action="/recipes/<%= Recipe._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete this recipes</button>
      </form>

      <a href="/recipes/<%= Recipe._id %>/edit">Edit this recipes</a>
      <% } else { %>
        <p>You don't own this, <%= Recipe.owner.username %> does!</p>
        <% } %>
</body>

</body>

</html>